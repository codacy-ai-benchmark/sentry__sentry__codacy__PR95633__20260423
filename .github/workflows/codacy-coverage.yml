name: Codacy Coverage
on:
  push:
    branches: ["main"]
  pull_request:
  workflow_dispatch:

jobs:
  coverage:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --prefer-offline

      - name: Run frontend tests with coverage
        run: pnpm exec jest --coverage --coverageReporters=cobertura --coverageDirectory=.artifacts/coverage --passWithNoTests
        continue-on-error: true

      - name: Upload coverage to Codacy
        uses: codacy/codacy-coverage-reporter-action@v1
        if: ${{ hashFiles('.artifacts/coverage/cobertura-coverage.xml') != '' }}
        with:
          project-token: ${{ secrets.CODACY_PROJECT_TOKEN }}
          coverage-reports: .artifacts/coverage/cobertura-coverage.xml
